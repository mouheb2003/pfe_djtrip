# DJTrip - Rapport du 2026-03-24

Ce document resume les changements effectues aujourd'hui, avec une verification des routes Back et de leur liaison avec le Front.

## 1) Verification des routes Back (liaison Front -> Back)

Verification faite sur:
- montage des routes dans `Back/server.js`
- definitions de routes dans `Back/routes/*.js`
- appels API dans `Front/lib/services/*.dart`

### Montage API dans `server.js`
Routes API v1 bien montees:
- `/api/v1/users`
- `/api/v1/auth`
- `/api/v1/touristes`
- `/api/v1/organisators`
- `/api/v1/activites`
- `/api/v1/inscriptions`
- `/api/v1/avis`
- `/api/v1/messages`
- `/api/v1/lieux`

### Cohérence Front Services -> Back Routes
Les endpoints utilises dans le Front existent bien cote Back:
- Auth/User: `signin`, `signup`, `forgot-password`, `reset-password`, `refresh-token`, `logout`, `users/me`, `users/:id`, `users/me/password`, `users/me/avatar`, `favorites`
- Auth secondaire: `/auth/verify-email`, `/auth/resend-verification`
- Activites: liste, details, `my-activities`, `archived`, suppression
- Inscriptions: creation, annulation, stats, approbation/refus, listes organisateur/touriste
- Messages: conversations, unread-count, `with/:partnerId`, edit, delete, audio, video
- Lieux: liste publique `/lieux` (et routes d'administration protegees)
- Avis: routes activite/organisateur/touriste + creation/suppression

### Point important apres evolution `app_routes` Front
`app_routes.dart` (navigation ecrans) n'impacte pas les endpoints HTTP Back.
La navigation nommee est cote UI; les appels reseau continuent de passer par `ApiClient` vers `/api/v1/...`.

## 2) Changements realises aujourd'hui (Front)

### Theme / Dark mode
- Ajout de `Front/lib/services/theme_service.dart` pour gerer et persister le mode clair/sombre.
- `Front/lib/main.dart` utilise `theme`, `darkTheme` et `themeMode` dynamique.
- `Front/lib/screens/shared/settings_screen.dart` active desormais le vrai switch Dark Mode avec sauvegarde.
- Ajout d'un theme sombre dans `Front/lib/theme/app_theme.dart`.

### Explore
- Remplacement de l'icone profil par l'avatar utilisateur dans l'en-tete de l'onglet Explore.
- Amelioration visuelle des filtres (chips plus lisibles en mode selection/non-selection).

### Partage profil et routage profond
- Generation d'un lien de profil partageable:
  - format: `djtrip://profile/<id>?type=tourist|organizer`
- Ajout d'une gestion de route profil dynamique dans:
  - `Front/lib/config/app_routes.dart`
- Mise a jour des boutons de partage dans:
  - `tourist_profile_tab.dart`
  - `public_organizer_profile_screen.dart`

### Messages / connectivite (online)
- Affichage du point de statut + texte `Online/Offline` dans la liste des conversations.
- Liaison sur `partnerOnline` avec mise a jour socket `user_status`.
- Correction de la normalisation URL socket/API pour les bases en `/api/v1`:
  - regex corrigee vers `/api(?:/v1)?$`
  - appliquee dans ecrans/messages et profils publics.

## 3) Validation technique rapide

- Verification statique de la coherence routes Back / appels Front: OK.
- Verification lint sur fichiers modifies: OK (pas d'erreur restante sur les fichiers touches).

## 4) Recommandation de verification manuelle (smoke test)

Pour confirmer en execution:
1. lancer Back et Front
2. tester login/signup + navigation nommee
3. tester ouverture Messages et statut online en temps reel
4. tester partage de profil (copie du lien)
5. tester filtres Explore + affichage avatar + Dark Mode persistant

